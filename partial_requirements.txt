There is a large area with buidlings. The buildings are categorized within zones. meaning Building A, B, C are in Zone One, Building D, E, F in Zone Two etc. 
In each building there are different water meters which has its identification number, and others. 
Concentrator 
------------------
TotalConsumption    
InstantaneousFlow
BatteryLevel        
Tamper
Leakage
ReverseFlow
BackflowEvents
MechanicalError 
MeterId 
ConcentratorId 
CreatedDateTime

----The above is the data coming for devices/meter or from concentrator. 

We will have zone in db:   

Zone
---------
ZoneId 
ZoneName 
DeviceNumbers


------------------------------------------------------------
Requirements: 
1. User Login portal 
	- Need to seed two admins - a. SuperAdmin, b. Admin
	- SuperAdmin has access to all things in the web app
	- Admin also has most of all access 
	- Username for admin: admin@system.com - pwd - EDMIN89@
	- Username for admin: sadmin@system.com - pwd - EDMINSA89@
	- Admin and Super Admin has feature to add users with new ui 
	- For that add email, username of the user, pwd will be generated by the app itself randomly 
	 with 8 length one capital, one lowercase, one number and one special characters
	- Once user is created it can be listed to the user list
	- in the user list there is option to acitivate/deactivate the user
	- once admin clicks activate, user can be allowed to sign in, for first sign in user is asked to reset password 
	- only then it can be used to add 
	- in the user list there is opton to change roles as well 
	- Roles should be like this: SuperAdmin, Admin, CollectiveAdmin, CollectiveUser, SingleUser, SingleAdmin
	- Collective admin should have access to multiple regions 
	- It can add users to multple regions, see data, download report and all 
	- SingleAdmin has accesss to single region and it is also mentioned in the user config. 
	- If user is has access to multiple region then after user pwd reset and always on login ask user to select the region -> select any one region.
	- If user has access to multiple region and after selecting a region user can switch to other region from user setting-> like selecting region in aws 
	
1-a - Main UI 
	- In the top navbar there will be the title of the site - ask for clarification
	- In the sidebar there will be buch of menus
	- For roles like SuperAdmin, Admin, CollectiveAdmin,SingleAdmin (come up with best names  for roles) - there will be option for User Management where there will be list of users and admin can add user, edit user, activate, deactiveate, change roles etc functionality 
	- For other users can not be changed such that their role can be changed to SuperAdmin or Admin; only others than this 
	- At the button there will be logout button 
	- At the top navbar there will be user icon at the right top when clicked there will be username, Full Name, Profile link and logout button 
	

2. Main Dashboard 
	- In the main dashboard i.e. when user logged in and select a region/ default region it will show the dashboard like 
	-  

3. Sub Dashboard 

4. MapView 
   - is it good to use leaflet with marking zone and other overlay like meters and clickable links and show show popup or user some pic map of the region and show things?
   

5. Report Download 
 
